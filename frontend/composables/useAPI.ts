// TODO: If response is 401 Unauthorized, redirect to login page (which does not exist yet but shhh)

export const useAPI: typeof useFetch = useFetch;
export const useLazyAPI: typeof useLazyFetch = useLazyFetch;
